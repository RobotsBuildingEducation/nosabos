# Local-only overrides can go here.
# Shared Vite config is read from .env.

# Keep Tutor on the Firebase AI Logic Gemini Live path. This local override
# prevents a stale OpenAI experiment flag from changing the provider.
VITE_TUTOR_REALTIME_PROVIDER=gemini
# Use Vertex AI backend for Gemini Live (stable 2.5 native-audio)
VITE_GEMINI_LIVE_PROVIDER=vertex
VITE_TUTOR_GEMINI_LIVE_MODEL=gemini-live-2.5-flash-native-audio

# Keep local OAuth/onboarding data in the Firebase emulators. The DEV guard in
# firebaseResources.js prevents production builds from ever using this value.
VITE_USE_FIREBASE_EMULATORS=true

/**
 * Real-world tasks constants.
 * Kept standalone to avoid bundling the RealWorldTasksModal component into the entry shell.
 */

export const REAL_WORLD_TASKS_REWARD_XP = 15;
export const REAL_WORLD_TASKS_REFRESH_MS = 6 * 60 * 60 * 1000; // 6 hours
